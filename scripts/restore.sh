#!/bin/bash
OUT=0;
for D in `find ./ -type f -name *.csproj`
do
    echo ========================================================
    echo ========================================================
	echo Testing: ${D}
	echo ========================================================
	echo ========================================================
    dotnet restore "${D}"
    OUT=$(($OUT+$?))
done

exit $OUT;