#!/bin/bash

for D in `find ./tests/ -type f -name *Test[s].csproj`
do
    echo ========================================================
    echo ========================================================
	echo Testing: ${D}
	echo ========================================================
	echo ========================================================
    dotnet test --no-build "${D}"
done